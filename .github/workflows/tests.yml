---
name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@1d96c772d19495a3b5c8eebd5c2cbf07bcccd908
        with:
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@bd47bffd8e9ea29bda42ffb202eadabecddf0e6b
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Run tests
        run: uv run pytest -v --cov --cov-report=term-missing --cov-report=html

      - name: Upload coverage report
        uses: actions/upload-artifact@50769540e7f41fca77a8e3f6cf8846566566f725
        if: matrix.python-version == '3.11'
        with:
          name: coverage-report
          path: htmlcov/

      - name: Check test coverage threshold
        if: matrix.python-version == '3.11'
        run: |
          coverage_percentage=$(uv run coverage report | grep TOTAL | awk '{print $4}' | sed 's/%//')
          echo "Coverage: ${coverage_percentage}%"
          if (( $(echo "$coverage_percentage < 70" | bc -l) )); then
            echo "Coverage ${coverage_percentage}% is below 70% threshold"
            exit 1
          fi

  test-preview:
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      matrix:
        python-version: ["3.15"]
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@1d96c772d19495a3b5c8eebd5c2cbf07bcccd908
        with:
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@bd47bffd8e9ea29bda42ffb202eadabecddf0e6b
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Run tests
        run: uv run pytest -v
